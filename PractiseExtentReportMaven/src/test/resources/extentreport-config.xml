<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
  <configuration>

    <!-- report theme -->
    <!-- STANDARD, DARK -->
    <theme>DARK</theme>

    <!-- document encoding -->
    <!-- defaults to UTF-8 -->
    <encoding>UTF-8</encoding>

    <!-- protocol for script and stylesheets -->
    <!-- defaults to https -->
    <!-- HTTP, HTTPS -->
    <protocol>HTTPS</protocol>

    <!-- offline report -->
    <timelineEnabled>true</timelineEnabled>
    
    <!-- offline report -->
    <enableOfflineMode>false</enableOfflineMode>
    
    <!-- use thumbnails for base64 images -->
    <!-- this may slowdown viewing tests -->
    <thumbnailForBase64>false</thumbnailForBase64>

    <!-- title of the document -->
    <documentTitle>Madhureddy</documentTitle>

    <!-- report name - displayed at top-nav -->
    <reportName>Madhu</reportName>

    <!-- timestamp format -->
    <timeStampFormat>DD-MM-YY -hh:mm:ss</timeStampFormat>

    <!-- custom javascript -->
    <scripts>
      <![CDATA[
        $(document).ready(function() {
            document.getElementsByClassName('logo')[0].style.display='none';
        });
      ]]>
    </scripts>

    <!-- custom styles -->
    <styles>
      <![CDATA[.badge-primary{background-color:grey}
      
      ]]>
    </styles>

  </configuration>
</extentreports>